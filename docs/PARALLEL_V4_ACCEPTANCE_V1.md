# parallel_v4 第一版验收记录

日期：2026-07-14

## 自动化结果

- `tests.test_parallel_v4` 与 `tests.test_pipeline_v3` 共13项测试通过。
- 覆盖非破坏性迁移、来源版本保留、严格扫描、保守归并、上下文硬预算、屏障提交、动态小岛流程、断点跳过、人工暂停与裁决、TXT/EPUB导出。
- `tests/test_foila_logic.py` 是依赖 `ARK_API_KEY` 的旧手工联网脚本，不属于自动化单元测试集合。

## Incandescence 影子试跑

- 迁移：112个文本块、309个旧术语、112个串行基线译文。
- 重复迁移前后，旧章节JSON文件哈希未变化。
- 严格预扫描：首块最终通过；模型前两次输出未通过JSON或枚举校验，第三次通过。无效输出未进入知识库。
- 两层翻译：首块初稿完整；两次润色均未通过段落数和长度校验，系统回退到完整初稿并标记 `completed_with_warnings`。
- 保持Thinking和 `reasoning_effort=high` 不变，将润色上限从6144提高到37200后强制重跑：润色一次生成完整21段、2008字，状态变为 `completed`，没有质量警告。该次润色耗时约55秒，隐藏推理约12129字符。
- 当前仅试跑1/112块，因此正式严格导出应当拒绝；这是预期行为。

人工对照文件：`projects/incandescence/exports/parallel_v4/shadow_comparison.md`。

## 第一版已知边界

- 尚未运行全书吞吐量和成本基准，不能据单块试跑断言达到全书性能目标。
- 首块确认6144的润色生成预算不足；37200能够容纳高强度思考和完整译文。完整性校验仍保留，遇到其他形式的残缺输出时继续回退并报警。
- 时间线、揭示边界、多重文学假说、Web裁决界面和局部句段修复留待第二版。

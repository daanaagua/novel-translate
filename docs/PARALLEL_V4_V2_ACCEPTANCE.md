# parallel_v4 第二版验收记录

验收日期：2026-07-14。

## 1. 自动化测试

执行命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_pipeline_v3 tests.test_parallel_v4 tests.test_parallel_v4_v2 -v
```

结果：24项测试全部通过。覆盖范围包括第一版串行与并行回归、章节真实顺序、DOCX幂等导入、揭示边界、高影响声明自动分流、扫描约束核验前禁止注入、双重独立核验、本机Web写令牌、局部修复版本保留、修复失败回退、注释默认排除和批准注释显式导出。

## 2. 《新日之书》旧DOCX基线

输入文件：`D:\llm\qikan4\2_Book of the New Sun_translated.docx`。

- 导入前SHA-256：`27135A6756BFB3B83567975009EE90688848B4BE082E245479A4095DDFA2353B`。
- 导入后SHA-256：`27135A6756BFB3B83567975009EE90688848B4BE082E245479A4095DDFA2353B`。
- 英文源文非空段落：11840。
- DOCX非空段落：11840。
- 已保存段落对应：11840。
- 已建立基线映射的文本块：510/510。
- 重复导入结果：`idempotent=true`。
- 有6个文本块的原文在全书中重复；导入器按章节真实顺序选择对应出现位置，并保留 `sequence_resolved_ambiguity` 标记，不伪装为唯一字符串匹配。

验收同时发现旧迁移器按文件名排列章节，会把 `v01_pre` 放到 `v01_ch30` 之后。第二版已改为按 `Chapter.index` 和 `Chunk.index` 排列，避免揭示位置、翻译屏障和基线映射使用错误书序。

## 3. Incandescence 冒烟验收

- 非破坏性迁移文本块：112。
- 旧串行译文记录：112。
- 当前第二版并行活动译文：1。
- 已生成单块影子对照报告。
- 原章节JSON、旧串行译文和旧导出均未改写。

## 4. Web与修复边界

- Web服务器固定绑定 `127.0.0.1`。
- 无随机令牌的POST请求返回HTTP 403。
- 局部修复严格要求响应段落数组与原文段落数一致。
- 修复成功时旧译文版本转为非活动，新译文作为活动版本；修复失败时旧译文保持活动并进入人工队列。
- 默认导出不含注释；显式启用时只导出 `approved` 注释。

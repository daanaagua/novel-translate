# FolioLoom 当前任务状态

## 2026-07-23 知识工作台 Goal（最新）

- 状态：完成，等待最终提交。
- 分支：`feat/folioloom-desktop`。
- 已完成：知识导入 SQLite 生命周期、运行时投影、严格 IPC/preload、知识工作台、内置导入向导、响应式样式，以及详情/关系/诊断查询下推。
- 性能加固：CSV 上限按“数据记录”计数，允许 1 个标题行加 100,000 条数据；staging 使用磁盘流式暂存、SQLite 聚合计数与有界分页，100,000 条实测 5.632 秒、峰值 RSS 约 316 MiB。
- 已修复真实 GUI 验收暴露的问题：暂存恢复分页上限漂移、丢弃后无法重试、终态 batch 混入恢复、视图切换焦点逃逸，以及大小写等价身份未绑定到已有规范实体导致提交失败。
- 性能加固已补充 active record identity 唯一部分索引，消除了逐条导入时扫描全部 active 记录的查询计划；50,000 条实测首屏 4.32 ms、后续页 3.20 ms、详情 1.17 ms，均低于 250 ms 门槛。整批提交约 173.9 秒；模糊搜索约 887 ms、诊断约 613 ms，作为非阻塞后台加载记录。
- 真实 Electron 验收又发现一次 Windows 临时只读 SQLite WAL/SHM 清理竞态会以 `ENOTEMPTY` 掩盖成功项目读取；现已集中重试并只忽略耗尽后的瞬时清理错误，快速重复打开回归通过。
- GUI 实机路径已跑通：portable 冷启动、知识列表/详情、人工修改、重启持久化、版本恢复、官方 JSON 冲突、别名合并提交、整批撤销、暂存重启恢复、焦点回收、丢弃后同文件重试；1442×922 与 1102×722 均无页面级横向或纵向溢出。
- 当前证据：核心 615 通过、1 项 Windows 符号链接跳过；desktop Node 73 通过、1 项同类跳过；renderer 53/53 通过；两套 TypeScript 检查、production build 与 Windows x64 portable 打包通过。
- 最终状态：README、两份验证报告、全量验证、真实 Electron GUI 验收与计划回填均已完成；本文件随最终提交入库。

## 2026-07-23 知识工作台 Goal 实时状态

- 已完成并提交：schema v3、字段级权威、原子知识命令、安全导入读取器、人工知识翻译投影、全局知识库、字段映射、冲突分类与导入准备服务。
- 已完成并提交：恢复翻译前自动同步较新的 book/project 知识，定向回归 42/42 通过。
- 已完成但待主代理复核：桌面可信知识服务、项目目标解析、全局库提升/附加编排。
- 正在执行：SQLite 导入 staging/提交/撤销、导入知识运行时投影修复、知识查询下推与大库性能加固。
- 核心审查发现 6 项 P1；恢复同步已修，剩余 global 不可变快照、导入对象真正生效、批量影响性能和查询性能正在逐项关闭。
- 后续关键路径：严格 IPC/preload → 知识工作台/导入向导 GUI → 全套测试、构建、打包和 GUI smoke。
- 最终验收：核心与桌面测试、两套 typecheck、production build、portable 打包、真实 Electron 交互全部通过。
- 2026-07-23 本轮续作：严格知识导入 IPC/preload 与主进程存储适配已接通；路径仅由主进程文件选择器持有，全部 payload 采用精确字段、枚举、UUID、分页和长度边界校验。
- 当前证据：核心 `npm run typecheck` 通过；桌面 IPC/安全定向测试 22/22 通过。知识工作台 UI 与导入向导仍在实现，尚未进入最终验收。

## 2026-07-23 当前 Goal：知识工作台与多格式导入

- **状态：** 执行中
- **分支：** `feat/folioloom-desktop`
- **规格：** `docs/superpowers/specs/2026-07-23-knowledge-workbench-and-import-design.md`
- **计划 A：** `docs/superpowers/plans/2026-07-23-knowledge-workbench-core.md`
- **计划 B：** `docs/superpowers/plans/2026-07-23-knowledge-import-wizard.md`
- **当前阶段：** 核心 Task 1–3 已通过风险审查并修复全部 3 个 P1/1 个 P2；正在接线分页查询、翻译投影与 catalog 同步。
- **验收门槛：** 核心测试、桌面测试、两套 typecheck、production build 全部通过；Electron GUI 实际打开并完成工作台/导入主路径 smoke。
- **本轮进度：**
  - schema v3 新建、v2 原子迁移、故障回滚和只读 v2 兼容：定向测试 29/29 通过。
  - 字段级 authority、旧 revision 哈希兼容和人工字段保护：定向测试 17/17 通过。
  - 原子知识命令、幂等 request、乐观锁、撤销、book/project catalog seed、模型 promotion generation：核心联合测试 58/58 通过；扩展命令测试 10/10 通过。
  - Task 1–3 风险审查发现并已修复：陈旧 run 虚报 catalog generation、人工快照破坏 lineage、六类字段缺少语义校验、authority/catalog 元数据未互证。
  - 修复后全套核心测试 542 通过、1 项 Windows 符号链接环境跳过；TypeScript 全量检查通过。
- 导入 Task 1–3 已完成严格 JSON/YAML/CSV/XLSX 读取、安全边界、显式编码协商与官方模板；定向测试 15/15 通过，等待后续字段映射与 staging 接线。
  - 导入 Task 5 纯领域部分已完成 TDD：确定性冲突分类、15 分钟/最多 4 个 pending 文件、可取消 operation 生命周期、inspect/suggest/stage 准备与存储适配边界；32 项导入联合测试及全量 typecheck 通过，不直接耦合 `LosslessBookStore`。

---

## 目标

- 修复 DeepSeek V4 Flash/high 连接测试误报，并统一连接探针与正式运行时策略。
- 完成桌面 GUI 端到端回归，修复模型配置、文件导入、试译和运行状态问题。
- 补齐日语、韩语的识别、章节结构、编码协商、分块估算与高效批量翻译。
- 用完整日语测试书与约 20 万字符韩语样本验证速度、完整性、稳定性和基础译文质量。

## 已完成

- 已建立 goal、设计规格与 TDD 实现计划。
- 已定位 DeepSeek 误报根因：旧探针只给 32 个输出 token，high 推理先耗尽预算，工具调用被 `finish_reason=length` 截断。
- 探针已复用正式运行时 wire policy，并对合法的 `length` 截断做一次有界扩容重试；真实 `deepseek-v4-flash` + `high` 探针约 5.5 秒通过流式、分段工具调用、往返、reasoning 连续性和 effort 五项检查。
- GUI 已修复保存模型回显、异步操作锁定、失败后保留未落盘密钥、忘记密钥后清理状态、切换服务商污染及旧试译泄漏等问题。
- 已增加完整的 `ja` / `ko` 语言画像、日韩句界、术语候选和残留检测；韩语额外识别 `제12화`、`12회`、`■ 标题 □` 等网文章节格式。
- 已加入 UTF/BOM/Shift-JIS/EUC-JP/EUC-KR/Windows-949 严格解码、候选评分、歧义确认和可追溯编码决策。
- 完整请求预算已覆盖系统提示、叙事记忆、原文、术语、风格、工具 schema、输出、推理预留和安全余量。
- 日韩分块已线性化；真实 691 万字符韩文源生成 3230 块约 2.1 秒并可精确重构。
- runner 已接入 quality/fast 双运行时、局部升级修复、类型化 provider 错误、请求数与在途 token 双约束 AIMD 调度和持久化调度快照。
- CLI 已支持 `--run-mode quality|fast` 与 `--max-in-flight-tokens`；quality 保持用户 effort，fast 首译使用低成本 effort，仅对失败或高风险片段升级。
- 已测得 DeepSeek 边际输入密度：日语约 1.30 字符/token，韩语约 1.41 字符/token；分块预算已使用语言与模型校准。
- 桌面端已接入显式“精细试译/快速试译”，IPC 只接受固定 mode；项目页显示日语/韩语、规范编码与置信度，不向 renderer 暴露项目内部路径。
- 快速模式的词汇锚定已改为先走低成本 primary，只有锚定失败时才局部升级，避免每个翻译波次都串行调用 high。
- 真实韩文规划发现并修复一项吞吐阻断：fast 允许 4,800-token 逻辑窗口，但旧物理请求默认仍限 3,200；两者现已统一，并以 3,203-token 韩文窗口集成测试锁定。
- 私有日韩基准 harness 已完成：默认 dry-run；20k 日/韩各做 quality/high 与 fast/off 对照，正式阶段为全量日文和约 20 万韩文 fast/off；持久报告不含正文、密钥、路径或原始响应。
- 桌面定向验证已通过：Node 61 通过、1 项因 Windows 文件符号链接环境跳过；renderer 24/24 通过；desktop typecheck 与 production build 通过。
- DeepSeek 工具调用第二轮若再次截断，现会沿用同一 deadline 做一次有界扩容，并明确返回 `PROBE_OUTPUT_TRUNCATED`，不再误报“不支持工具调用”。
- 桌面源文件复用现会重新执行当前语言/编码策略；旧 `und` 韩文项目和缺失编码决策的旧纯文本项目不会再被静默复用。
- 请求预算已把真实 provider usage 回灌到“模型 × 语言画像”校准作用域，后续波次会自动修正 token 估算，其他模型不受污染。
- renderer 已用 generation 拒绝迟到的旧 onboarding 快照，启动刷新不再覆盖刚导入的新书稿；EPUB/DOCX 也改为读者可理解的“正文已提取”提示，不再显示 `zip-container`。
- 翻译请求只发送与当前片段文字命中的有界知识投影（最多 24 条、24 KB）；完整 SQLite 知识历史保持不变，千级与 5900 条韩语快照均可在 128k 上下文内准入。
- provider 上下文超限现会先按逻辑窗口、再按不可变 block 边界有界拆分；同一逻辑窗口只 claim/stage/promote 一次，运行时 context 错误不会错误触发高思维升级。
- 对模型抄错一位的规范 block ID，现只在“未知、同长度、唯一汉明距离 1、且目标是当前窗口真实块”时机械恢复并留审计 warning；真实其他块、两位差异与歧义仍严格拒绝。
- 新旧翻译路径现共用状态机式中文引号归一化；初稿与定向修复均在校验前处理 ASCII、日式及非规范双引号，额外闭引号仍由边界校验拦截。
- 目标语已在共享常量、请求载荷、系统提示和默认文体宪章中统一为简体中文 `zh-Hans`，避免长篇韩文在并行窗口间切换繁简体。
- 核心全量回归现为 396 通过、1 项因 Windows 无符号链接能力而跳过、0 失败。

## 进行中

- 完整日文 fast/off 与约 20 万字符韩文 fast/off 正式基准均已完成且通过确定性完整性检查，正在进行独立译文抽检、quality/high 结构兜底实测及桌面端最终验收。

## 待验收

1. 跑完整日语文本和约 20 万字符韩语文本的真实 API 基准，抽检首中尾译文。
2. 运行核心、桌面、构建、打包及 Electron smoke 的全部回归。
3. 汇总问题、速度、质量与剩余风险，完成最终提交。

## 2026-07-22 最新进展

- 真实韩文试跑暴露的繁体漂移已改为确定性输出归一化：OpenCC 在校验前统一转为简体，同时逐字保护用户锁定的术语目标形式。
- 韩语词汇候选现在按助词词形归并（如 `묵향은/의/을` 合并为 `묵향`），并用词形证据与汉字释义线索削减谓词噪声。
- fast 模式的取数范围扩大到两倍调度并发，但实际并发上限不变；出现重试、实体警告或 `needs_revalidate` 后自动退回单倍范围。
- 上述定向测试与核心 typecheck 已通过，下一步重跑真实韩文小样并据测量继续定位吞吐瓶颈。
- 真实 20k 韩语基准确认大型译文 JSON 工具调用是主要吞吐瓶颈；fast 模式现改用请求专属 nonce 的无工具分帧正文协议，quality 模式继续保留结构化工具。
- 分帧协议严格拒绝错误 nonce、未知/重复/缺失块和标记外正文；所有结果仍复用原有集合校验、繁简/引号归一化、定向修复和原子提交，结构失败按不可变 block 有界切分。
- DeepSeek V4 Flash/off 的最新韩语 20k 真实试跑 63.5 秒完成，7 次模型调用、0 重试、11/11 blocks、覆盖率 100%、0 incident、0 protocol violation，约 111.9 万源字符/小时，较旧结构恢复方案的 383 秒和 19 次调用显著改善。
- 模型生成的术语锚点与实体目标在入库前确定性转为简体；历史 knowledge 术语在读取时兼容归一化，用户 glossary/legacy 锁定形式仍逐字保留。最新译文经 OpenCC 全文复核无可转换繁体残留。
- lossless 纯译文导出不再重复插入源语言章节元数据；翻译后的标题块仍保留，韩语标题和 `chapter-at-*` 合成标识不再泄漏到最终 TXT。
- 完整日文首轮形式审计发现 `拜み讨ち` 这类“假名被汉字隔开”的混合残留未被旧的连续双假名规则识别；画像现增加有界混合词检测并尊重显式保留项，定向修复回归已覆盖。
- 完整日文最终基准：74,703 源字符、61/61 blocks、48 次模型调用、0 重试、0 incident、覆盖率 100%，耗时 484.8 秒，约 55.5 万源字符/小时；输出通过简体、日韩残留、乱码、引号平衡检查。
- 约 20 万韩文最终基准：195,578 规范源字符、97/97 blocks、54 次模型调用、0 重试、0 incident、覆盖率 100%，耗时 759.9 秒，约 92.7 万源字符/小时；输出通过简体、韩文残留、乱码、引号平衡检查。
- quality/high 在结构化工具载荷缺失或畸形时，现仅把失败窗口切换为同一 high 升级运行时的 nonce 分帧正文协议；已经成功的同批窗口不会重译，若分帧仍失败才按不可变 block 边界有界拆分。定向回归覆盖单窗口和“保留有效兄弟窗口”两种情形。
- 独立韩文语义抽检发现旧正式样本存在两项 P0：`[[]]` 提取场景分隔符后的正文被静默遗漏，以及相邻 block 的整段译文串块重复。旧样本已判定不可交付，不能用“覆盖率 100%”替代语义对齐验收。
- 修复采用通用对齐约束而非书名特判：提取场景标记现在形成不可变硬边界；模型复制或简化出的 `[[]]` / `[]` 在译文中统一还原为空行；日/韩长块增加按语言标定的逐块长度区间；不同源块的长段目标文本重复会触发一次定向修复。
- 新增校验已覆盖跨逻辑窗口重复、单块异常缩短/膨胀、场景标记清理和分块无损重构；同时去重同一修复失败，避免重复证据污染修复提示。定向回归 117 通过、1 项 Windows 符号链接能力跳过、0 失败，核心 typecheck 通过。
- 下一步：在新画像与对齐校验下重跑约 20 万字符韩文正式样本，再做独立语义复核；随后运行核心、桌面、构建和 portable 打包全量回归。

## 2026-07-22 终验加固

- 已停止旧韩文正式样本的交付使用；该样本虽覆盖率为 100%，但独立抽检发现跨场景遗漏和相邻块串译，已作为 P0 回归样本处理。
- 无损原文与模型可见文本现正式分层：`[[]]` 等提取布局只保留在账本、哈希和坐标中，所有翻译、修复、问题侦察、词汇锚定与证据工具只看到普通段落边界；预算估算与实际请求使用同一投影。
- 场景布局不再按目标文本位置猜测并删除 `[]`/`[[]]`；歧义字面量保持原样，若模型泄漏源布局 token，则确定性校验触发局部修复。
- 分块器增加语义 scalar 前缀索引与 layout span 二分保护；超长空白、连续场景标记和 token 边界均保持线性处理、无损重构且模型可见 token 不超预算。
- 完整性校验已改为逐块分段长度带，并覆盖所有语言画像；长度按 grapheme 计数，同时拒绝零宽/C0/U+FFFD/noncharacter/孤立 surrogate/PUA 填充、纯标点数字表情伪译和非中文脚本伪译。
- 跨块长段重复检测已移除 peer 完全图，错误 group 按块生成固定长度摘要，时间与内存由平方级恢复为近线性；证据 quote 同样使用模型可见投影和投影后预算。
- GUI 连接测试失败会在按钮旁立即反馈并保留未保存 API Key；无活动模型时重启会显示最近一次失败探针，不再表现为无提示重置。
- 当前定向回归：核心/模型可见/证据/日韩完整性 76 项通过；桌面 Node 64 项通过、1 项因 Windows 无符号链接能力跳过；renderer 28/28 通过；核心与桌面 typecheck 通过。
- 正在等待三路独立最终审计；随后重跑约 20 万韩文正式样本、复核完整日文产物并执行完整构建/portable 打包。
- 新版韩文正式样本已在严格段落一一对齐、源感知引号归一化、软/硬术语锚点和退化窗口隔离重试下完成：200,460 字符前缀（规范正文 195,578 字符）、113/113 blocks、42 个逻辑窗口、70 次请求、1 次局部重试、耗时 1,213.0 秒，约 58.0 万规范源字符/小时。
- 韩文离线独立完整性审计结果为 113/113 active translations、0 校验失败、0 源文残留、0 乱码提示、0 非法 Unicode/控制符，所有块的源/目标段落数严格相等；正在等待人工语义抽检确认曾出错的 57/58/93/102/103 块。
- 下一步正在用同一最终代码重跑完整日文正式基准，随后执行真实 high 探针、全量核心/桌面/构建/portable 终验。

## 2026-07-22 最终验收

- DeepSeek V4 Flash / high 真实连接探针 4.62 秒通过；流式、工具调用、工具往返、reasoning 连续性和 effort 五项均为 passed。
- 完整韩文终验样本为 200,460 原始字符、195,578 规范正文字符；113/113 blocks、42 个逻辑窗口、70 次请求、1 次局部重试，约 1,213 秒，完整性和乱码/残留检查全部通过。
- 完整日文测试书为 74,703 字符、61/61 blocks；严格终验重跑约 1,189.5 秒，完整性和乱码/残留检查全部通过。
- 韩文词汇定向复测确认“墨香”“龙天益”“赫武相”跨块一致；无汉字证据的人名只建立可修订软锚点，不做错误硬锁定。
- 核心测试 489 通过、0 失败、1 跳过；桌面 Node 测试 64 通过、0 失败、1 跳过；renderer 30/30 通过；核心与桌面 typecheck、production build、Windows x64 portable 打包及打包产物冷启动全部通过。
- portable 产物：`folioloom/release/FolioLoom-portable-win-x64.exe`。
- 三路独立审查均已完成：未发现 P0/P1；剩余 P2 为低置信度别名的独立全书再验证，以及 Flash 文学质量需在正式模型发布后重新盲评。
- 详细报告：`folioloom/docs/superpowers/reports/2026-07-22-cjk-desktop-validation.md`。
